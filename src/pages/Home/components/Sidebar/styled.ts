import styled from "@emotion/styled";

const FilterTitle = styled("span")`
  font-size: 14px;
  font-weight: 700;
`;

const FilterItem = styled("div")`
  margin-bottom: 39px;
`;

const Styled = { FilterTitle, FilterItem };

export default Styled;
